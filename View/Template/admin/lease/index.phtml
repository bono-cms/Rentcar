<?php

use Krystal\Form\Element;
use Krystal\Widget\GridView\GridViewWidget;
use Cms\View\Icon;

defined('KRYSTAL') or die;

?>

<form>
    <div class="row">
        <div class="col-lg-1">
            <div class="btn-toolbar">
                <div class="dropdown">
                  <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
                    <i class="glyphicon glyphicon-list"></i> <?php $this->show('Menu'); ?> <span class="caret"></span>
                  </button>

                  <div class="dropdown-menu">
                     <a class="dropdown-item" href="<?= $this->url('Rentcar:Admin:Lease@addAction'); ?>"><i class="glyphicon glyphicon-file"></i> <?php $this->show('Add new contract'); ?></a>
                     <div class="dropdown-divider"></div>
                     <div class="dropdown-header"><?php $this->show('Actions'); ?></div>
                     <?php if (!empty($contracts)): ?>
                     <a class="dropdown-item" href="#" data-button="remove-selected" data-url="<?= $this->url('Rentcar:Admin:Lease@deleteAction', null); ?>"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('Remove selected'); ?></a>
                     <?php else: ?>
                     <a class="dropdown-item disabled"><i class="glyphicon glyphicon-floppy-save"></i> <?php $this->show('Save changes'); ?></a>
                     <a class="dropdown-item disabled"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('Remove selected'); ?></a>
                     <?php endif; ?>
                  </div>
                </div>
            </div>
        </div>
    </div>

    <div class="card">
        <h4><i class="glyphicon glyphicon-th-list"></i> <?php $this->show('Contracts'); ?> (<?= count($contracts); ?>)</h4>

        <div class="table-responsive">
            <?= $this->widget(new GridViewWidget($contracts, array(
                'tableClass' => 'table table-hover table-bordered',
                'columns' => array(
                    array(
                        'column' => 'id',
                        'label' => '#'
                    ),

                    array(
                        'column' => 'owner'
                    ),

                    array(
                        'column' => 'model'
                    ),
                    
                    array(
                        'column' => 'numberplate'
                    ),
                    
                    array(
                        'column' => 'contract_number'
                    ),

                    array(
                        'column' => 'apply_date'
                    ),
                    
                    array(
                        'column' => 'run_date'
                    ),
                    
                    //
                ),

                'actions' => array(
                    'edit' => function($row){
                        return Icon::edit(
                            $this->url('Rentcar:Admin:Lease@editAction', $row['id']),
                            $this->translate('Edit this contract')
                        );
                    },

                    'remove' => function($row){
                        return Icon::remove(
                            $this->url('Rentcar:Admin:Brand@deleteAction', $row['id']),
                            $this->translate('Remove this contract')
                        );
                    }
                )
            ))); ?>

            <?php if (empty($contracts)): ?>
            <div class="center-block">
                <h2 class="text-center text-muted"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('No contracts'); ?></h2>
            </div>
            <?php endif; ?>
        </div>
    </div>
</form>

